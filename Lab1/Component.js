class Component {
  constructor(props, state) {
    this.props = props,
    this.state = state
  }

  setObject(object) {
    this.state = object;
  }

  render() {

  }
}

module.exports = Component;
